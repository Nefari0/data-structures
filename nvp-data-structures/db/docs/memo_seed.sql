
-- CREATE TABLE data_memos (
--   memo_id SERIAL PRIMARY KEY,
--   title VARCHAR(250),
--   body text,
--   category VARCHAR(250)
-- )

-- INSERT INTO ddata_memos (title,body)
-- VALUES (
--   'first memo',
--   'this is the first memo written  for CDL data Structures'
-- )